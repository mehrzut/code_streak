import 'package:flutter/material.dart';

class ContributionCalendarWidget extends StatelessWidget {
  const ContributionCalendarWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
